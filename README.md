# Angular CRUD Demo

This repository contains two Angular applications:

1. Based on a shared "upsert" component for both "adding" and "updating" users
2. Based on separate components for "adding" and "updating" users (following SOLID principles).
